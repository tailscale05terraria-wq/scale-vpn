name: scale_vpn
description: "Steampunk V2Ray/Xray VPN client for Android"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Официальный сетевой движок Xray-core и Android VpnService
  flutter_v2ray: ^1.0.10
  http: ^1.2.1
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
